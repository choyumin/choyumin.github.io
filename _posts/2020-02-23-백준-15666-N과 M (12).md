---
layout: single
title: "[백준] 15666. N과 M (12)"
comments: true
category: Baekjoon
---

## 15666.  N과 M (12)

---

### Code

```java
import java.io.*;
import java.util.*;

public class N과M12 {
	static StringBuilder sb;
	static int[] arr;
	static int[] sel;
	static LinkedHashSet<String> lhset;

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
		sb = new StringBuilder();

		String[] s = br.readLine().split(" ");
		String[] str = br.readLine().split(" ");

		int N = Integer.parseInt(s[0]);
		int M = Integer.parseInt(s[1]);

		arr = new int[N];
		sel = new int[M];

		for (int i = 0; i < N; i++)
			arr[i] = Integer.parseInt(str[i]);

		Arrays.sort(arr);
		lhset = new LinkedHashSet<>();

		multi_combi(0, 0);
		Iterator<String> iter = lhset.iterator();

		while (iter.hasNext()) {
			bw.write(iter.next());
			bw.write("\n");
		}
		bw.close();
	}

	private static void multi_combi(int idx, int s_idx) {
		if (s_idx == sel.length) {
			for (int i = 0; i < sel.length; i++) {
				sb.append(sel[i] + " ");
			}
			lhset.add(sb.toString());
			sb.delete(0, sb.length());
			return;
		}

		for (int i = idx; i < arr.length; i++) {
			sel[s_idx] = arr[i];
			multi_combi(i, s_idx + 1);
		}
	}
}

```
